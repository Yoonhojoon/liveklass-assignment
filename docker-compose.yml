services:
  app:
    build:
      context: .
    image: liveklass-demo:local
    ports:
      - "${APP_PORT:-8080}:8080"
    environment:
      SPRING_PROFILES_ACTIVE: docker
    volumes:
      - liveklass-h2-data:/app/data

volumes:
  liveklass-h2-data:
